{
  "name": "outline-i18n",
  "version": "0.0.7",
  "license": "Apache-2.0",
  "scripts": {
    "build": "tsc -p .",
    "clean": "rm -rf node_modules/",
    "precommit": "tslint -p . --fix; git-clang-format --extensions ts"
  },
  "devDependencies": {
    "clang-format": "^1.2.2",
    "husky": "^0.14.3",
    "tslint": "^5.9.1",
    "typescript": "^2.7.2"
  }
}
